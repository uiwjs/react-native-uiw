import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  item: {
    minHeight: 50,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
